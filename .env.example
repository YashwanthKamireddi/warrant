# Razorpay test mode. Leave unset to run entirely on the simulated rail.
RAZORPAY_KEY_ID=rzp_test_xxxxxxxxxxxx
RAZORPAY_KEY_SECRET=

# Claude powers exactly two jobs: scope derivation and semantic divergence.
# Unset -> the engine falls back to its deterministic-only path and says so.
ANTHROPIC_API_KEY=
WARRANT_MODEL=claude-sonnet-5
