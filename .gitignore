__pycache__/
*.py[cod]
.venv/
.env
*.db
*.db-journal
node_modules/
dist/
.ruff_cache/
.pytest_cache/
out/

shots/

# Private strategy notes — never committed. Judges read this repo.
notes/

# Video: the scripts and narration are source, the footage is build output.
.video/clips/
.video/work/
.video/out/

# TypeScript build cache. Local, machine-specific, and it was tracked.
*.tsbuildinfo

# Submission material. Written for the people judging this, and handed to them
# directly -- form answers and a script to read aloud are not part of the
# product, and a repository is not where either belongs.
SUBMISSION.md
.video/NARRATION.md
